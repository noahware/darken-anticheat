namespace Anticheat;

table NmiCoreCapture {
    rip: uint64;
    cs: uint64;
    processed: bool;
    process_id: uint32;
}

table NmiResult {
    captures: [NmiCoreCapture];
    current_core: uint32;
}
