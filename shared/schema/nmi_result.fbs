namespace Anticheat;

table NmiCoreCapture {
    rip: uint64;
    cs: uint64;
    processed: bool;
    return_addresses: [uint64];
}

table NmiResult {
    captures: [NmiCoreCapture];
    current_core: uint32;
}
