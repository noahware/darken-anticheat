#pragma once
#include <optional.hpp>

namespace std
{
    template <class T>
    using optional = cstd::optional<T>;

    struct nullopt_t
    {
        explicit constexpr nullopt_t(int) {}
    };
    inline constexpr nullopt_t nullopt{0};
}
